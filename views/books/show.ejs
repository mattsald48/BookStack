<h2 class="page-header"><%= book.title %></h2>
<img
  width="100"
  height="150"
  src="<%= book.coverImagePath %>"
  alt="<%= book.title%> Cover Image"
/>
<div>Author: <%= book.author.name %></div>
<div>Publish Date: <%= book.publishDate.toDateString() %></div>
<div>Page Count: <%= book.pageCount %></div>
<div>Description: <%= book.description %></div>

<% if (book.isRead) { %>

<div>
  <h2>
    This book is no longer on the Stack. Go ahead and put it on the shelf and
    get started on another!
  </h2>
</div>
<% } else { %>
<div>
  <h2>
    This book is currently on the stack. You've been working hard, how about you
    treat yourself to a nice break and get to reading?
  </h2>
</div>
<% } %>

<a href="/books/<%= book.id %>/edit">Edit</a>
<%- include('../partials/deleteForm', { url: `/books/${book.id}` }) %>
<a href="/authors/<%= book.author.id %>">View Author</a>
